<div class="card card-step shadow-sm h-100 p-4 text-center">
    <i class="{{ $icon }} step-icon {{ $iconColor ?? 'text-primary' }}"></i>
    <h5 class="fw-bold">{{ $title }}</h5>
    <p class="text-muted small">{{ $description }}</p>
</div>